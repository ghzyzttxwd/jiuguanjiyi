// Canary companion bootstrap.
// It does not load the archive core and does not mutate MVU on startup.
// It only exposes an explicit RC loader after the stable Variable Archive Bridge is present.

import './canary_loader.js';
