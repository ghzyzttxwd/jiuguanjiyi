# Mobile Canary 验收计划（变量归档桥 v0.2 RC）

> 目标：在不污染稳定版、不把实验代码接入正常启动链的前提下，完成 Android SillyTavern 实机验证。

## 0. 发布隔离

- 稳定版继续使用 `main` / v0.1.4。
- RC 开发分支：`rc/auto-memory-v0.2`。
- 手机 Canary 伴侣分支：`canary/mobile-v0.2`。
- Canary 伴侣自身不加载 `index.js`，不创建第二套 VariableArchiveBridge 核心，只依赖已经运行的稳定版。
- Canary 伴侣启动时只加载 `canary_loader.js`；实验模块必须用户主动点击后才动态 import。
- Canary `auto_update=false`，避免测试过程中被远端变更自动替换。

## 1. Canary A：启动与卸载（零写入）

验收动作：
1. 保持稳定版 v0.1.4 正常安装。
2. 额外安装 Canary 伴侣。
3. 完整关闭并重新打开 SillyTavern。
4. 不点击“加载统一主控RC”，正常进入任意变量卡聊天。
5. 连续重启两次。
6. 删除 Canary 伴侣，再重启一次。

通过标准：
- 启动速度无明显异常；
- 稳定版变量归档桥仍能识别 MVU；
- 不产生任何自动归档/恢复/Prompt 注入；
- 删除 Canary 伴侣后稳定版完全不受影响。

失败处理：
- 只删除 Canary 伴侣扩展目录；不要清应用数据、不要删除稳定版、不要重置用户资源。

## 2. Canary B：加载最小 RC 栈（自动功能仍关闭）

验收动作：
1. 点击“加载统一主控RC”。
2. 不开启统一主控。
3. 点击“统一安全预检”。
4. 点击“Canary诊断 → 生成诊断”。
5. 正常滚动设置面板、切换聊天、返回聊天界面。

通过标准：
- 页面不卡顿；
- 只出现三层主要组件：召回、生命周期、统一主控，以及被动诊断；
- 统一主控仍关闭；
- 诊断报告不包含聊天正文、角色名、变量内容、变量子键或 scope ID；
- 设置面板重绘后组件能够重新出现，不重复绑定出多份实例。

## 3. Canary C：只开统一主控，但暂不制造归档条件

验收动作：
1. 在已有变量卡聊天中点击统一主控总开关。
2. 只确认一次。
3. 正常聊天 3～5 个来回。
4. 中间切换一次聊天，再切回来。
5. 生成 Canary 诊断。

通过标准：
- Prompt 召回与生命周期子系统同时开启；
- 主控开启期间子开关被锁住；
- 模型生成期间不发生 MVU 写入；
- 聊天切换首轮不写；
- 没有达到归档条件时不做无意义迁移；
- `recentErrors=0`，`failClosedCount=0`；
- 关闭主控后先停止生命周期写入，再清空本插件拥有的 Prompt。

## 4. Canary D：真实热→冷归档

只使用可丢弃/可恢复的测试节点，禁止第一次就拿核心主角状态、当前任务、当前世界、队伍等热节点测试。

验收前：
- 手动保存一次 MVU 快照；
- 确认稳定版手动“归档 → 恢复”链路仍可用；
- 测试节点必须位于动态对象容器下。

验收目标：
- 自动归档前生成快照；
- 冷档案完整保存；
- 热变量只删除目标直接子项；
- Schema 若自动补回节点，自动归档必须判失败；
- 每个稳定周期最多修改一个 MVU 节点；
- 30 秒冷却有效。

## 5. Canary E：Prompt 召回

场景：目标已经在冷档案中，聊天再次提到目标名称/明确相关标签。

通过标准：
- 冷档案只注入相关记录；
- 同一节点已经在当前 MVU 时不重复注入；
- 检测到 `{{varArchiveContext}}` 已在当前 Prompt 时自动让路；
- 检测到记忆增强镜像时跳过重复镜像；
- `vab_cold_recall` 被外部内容占用时拒绝覆盖并关闭召回；
- 单纯“提到旧人物/武学”不得硬恢复 MVU。

## 6. Canary F：重激活

场景：冷档案节点在模型正常更新后重新出现在 MVU。

通过标准：
- 至少连续两次稳定观察后才处理；
- 生成期间不处理；
- scope / 楼层 / 目标节点发生竞态时中止；
- 异常达到阈值后熔断；
- 自动模式不得因为“仅被提到”而恢复；
- 自动模式对冷旧字段必须采用保守策略，禁止无条件把可能已经过期的冷字段重新写回当前 MVU；
- 需要真正冷→热字段合并的激进模式只能保留为高级/人工事务，不作为日常自动默认。

## 7. Canary G：长时间运行

目标：30～60 分钟、至少 30 次模型生成/更新事件。

观察：
- WebView 无持续升高的卡顿；
- 没有 MutationObserver 自触发；
- 事件处理使用 debounce；
- 生命周期没有固定高频扫描；
- 主控健康检查以事件触发为主，15 秒 watchdog 只作兜底；
- 关闭/重新打开主控不会重复注册事件监听；
- 设置面板多次重绘不会解除主控对子开关的锁定。

## 8. 持久自动模式（最后阶段，当前不启用）

只有 A～G 全部通过后才允许设计“重启后自动开启”。

必须先启用 Boot Guard：
- 自动启动前写 `starting` 标记；
- 经过健康宽限期后才写 `healthy`；
- 若上次停在 `starting`，下一次启动直接进入 safe mode，不自动加载写入引擎；
- safe mode 只能由用户明确解除；
- 自动模式意图与启动健康状态分开保存，不能因为持久开关为 ON 就绕过安全模式。

## 9. 合并门槛

以下条件全部满足后，Draft PR 才能考虑进入生产主分支：
- GitHub CI 全绿；
- Android Canary A～G 全通过；
- 至少一次真实自动归档成功；
- 至少一次真实 Prompt 冷档案召回成功；
- 至少一次重激活安全收口成功；
- 至少一次故障/冲突场景能 fail-closed；
- Canary 伴侣可单独删除恢复；
- 正式版启动链仍有独立回滚路径。
